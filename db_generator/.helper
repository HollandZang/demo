
Helper
   System command:
       helper     user guide
       exit       exit this cmd
   Database command:
       tables     get the name of all the tables
   Generate parameter: <database table name> [params...]
       -a,-all    generate all, include 'pojo', etc...
       -m,-model  generate pojo
